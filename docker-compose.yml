services:
  postgres:
    image: postgres:18
    environment:
      POSTGRES_USER: cristiano41
      POSTGRES_PASSWORD: cristiano2009
      POSTGRES_DB: inventario
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql

volumes:
  postgres_data:
